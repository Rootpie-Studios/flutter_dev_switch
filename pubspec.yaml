name: flutter_dev_switch
description: Runtime API server switching, a TestFlight/debug-only dev menu and the widgets that open them. Shared by the Rootpi Flutter apps.
version: 0.9.0
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  dio: ^5.8.0
  flutter:
    sdk: flutter
  package_info_plus: '>=9.0.0 <11.0.0'
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
